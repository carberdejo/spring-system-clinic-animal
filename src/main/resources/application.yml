spring:
  application:
    name: ProyClinicAnimal
  datasource:
    url: jdbc:mysql://localhost:3306/ClinicAnimal
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
